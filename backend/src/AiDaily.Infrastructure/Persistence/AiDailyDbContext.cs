namespace AiDaily.Infrastructure.Persistence;

public sealed class AiDailyDbContext
{
    // Placeholder boundary for S1. Later slices can replace the in-memory repository
    // with EF Core without changing API controllers or query services.
}
